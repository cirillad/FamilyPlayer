<Window x:Class="MusicPlayer.SongSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Song" Height="400" Width="300">
    <Grid>
        <ListBox Name="SongListBox" Margin="10"/>
        <Button Content="Play" Click="PlayButton_Click" Margin="10,0,10,10" HorizontalAlignment="Right"/>
    </Grid>
</Window>
